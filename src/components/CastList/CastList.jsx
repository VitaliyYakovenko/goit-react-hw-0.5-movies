


export default function CastList({cast}) {
    console.log(cast);
 
    
    // return (<ul>
    //     {cast?.cast.map(({ id, original_name, profile_path, backdrop_path }) =>
    //         <li key={id}>
    //             <p>{original_name}</p>
    //             <img src={profile_path || backdrop_path} alt={original_name} />
    //         </li>)}
    // </ul>);
};